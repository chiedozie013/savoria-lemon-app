import { Fragment } from "react";

export default function NotFoundPage() {
  return (
    <Fragment>
      <div className="fluidContainer">
        <h2 className="sectionTitle">Oups! This item does not exist!</h2>
      </div>
    </Fragment>
  );
}
